import { FaceEmotion, VoiceEmotion } from "@/types/emotion";
export const detectFunc = ({
  faceEmotion: voiceEmotion,
}: {
  faceEmotion: FaceEmotion;
  voiceEmotion: VoiceEmotion;
}): boolean => {
  // TODO:ここに判別の式を書く
  return false;
};
